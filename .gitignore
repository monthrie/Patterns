build/
node_modules/
.playwright-mcp/
scripts/artifacts/
.claude/
.DS_Store

# secrets — never commit
.env

# heavy media — organized under media/ (zips = raw backups); knowledge stays in docs/
media/
batchonevideos/
*.zip
*.crdownload

# full photo library + derived sheets are gitignored (raw backups live in media/zips/)
docs/photos/**/*.jpg
docs/photos/**/*.jpeg
docs/photos/**/*.png
docs/interviews/contact-sheets/

# NOTE: docs/outreach/riga-triennial/candidate-images/ is intentionally NOT ignored
# (small curated email kit, version-controlled deliberately after a data loss)
